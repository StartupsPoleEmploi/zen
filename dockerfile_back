FROM node:8.11.1

WORKDIR /home/back

CMD if [ "$NODE_ENV" = "production" ]; then \
        npm install --only=production && \
        npm run knex migrate:latest && \
        npm run production; \
    else \
        npm install && \
        npm run knex migrate:latest && \
        npm start; \
    fi
