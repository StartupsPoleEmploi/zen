FROM node:8.11.1

WORKDIR /home/back

CMD if [ "$NODE_ENV" = "production" ]; then \
        npm install --only=production && \
        npm run db:migrate && \
        npm start; \
    else \
        npm install && \
        npm run db:migrate && \
        npm start; \
    fi
